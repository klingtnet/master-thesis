\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{tikz}[2016/06/28 TikZ styles for my master's thesis (Andreas Linz)]

% TikZ definitions for DSP block diagrams
% Source: http://www.texample.net/tikz/examples/noise-shaper/
\tikzset{%
  >=latex,
  font=\sffamily,
  block/.style    = {draw, thick, rectangle, line width=1pt, minimum height = 3em,
    minimum width = 3em},
  sum/.style      = {draw, circle}, % Adder
  conn/.style     = {draw, circle, radius=1em, fill=black},
  empty/.style    = {},
  input/.style    = {draw=none, fill=none}, % Input
  output/.style   = {draw=none, fill=none}, % Output
  % MIDI
  msg/.style 	    = {align=center, rectangle, rounded corners, draw, font={\sffamily\footnotesize}},
  % OSC
  osc-method/.style = {font={\ttfamily\itshape}}
}
% addition
\providecommand{\suma}{\Large$+$}
% integrator
\providecommand{\inte}{$\displaystyle \int$}
% differentiator
\providecommand{\derv}{\huge$\frac{d}{dt}$}
